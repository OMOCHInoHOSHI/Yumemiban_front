// ニックネームに対する処理を行う関数
export const pushNickname = (nickname: string) => {
    console.log('Nickname:', nickname);
};

// 夢の内容に対する処理を行う関数
export const pushDreamtext = (dreamContent: string) => {
    console.log('Dream Content:', dreamContent);
};

// ニックネームを取得する関数
export const getNickname = () => {
    return 'nickname';
}